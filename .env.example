# Supabase — copy values from `supabase start` output for local dev,
# or from your Supabase project dashboard for dev/prod.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-side only — NEVER expose to the browser
SUPABASE_SERVICE_ROLE_KEY=

# OpenAI
OPENAI_API_KEY=

# Early access beta code (server-side only)
EARLY_ACCESS_CODE=
